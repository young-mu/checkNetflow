This is a monitor routine.

It will check netflow and balance from hitsun.hit.edu.cn, once the balance reaches threshold,this routine will automatically send email to everyone in this Lab.


(i). getHTML.py can get html content by logging in hitsun.hit.edu.cn, the username & passwordare stored in loginfo.py, which is not included by git.

(ii). ...
